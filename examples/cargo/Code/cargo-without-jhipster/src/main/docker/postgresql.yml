version: '2'
services:
  cargo-postgresql:
    image: postgres:11.3
    # volumes:
    #     - ~/volumes/jhipster/cargo/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=cargo
      - POSTGRES_PASSWORD=
    ports:
      - 5432:5432
